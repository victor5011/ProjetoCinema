@model ReflectionIT.Mvc.Paging.PagingList<Pedido>

@{
    ViewData["Title"] = "Index";
}

<h1>Pedidos</h1>

<form method="get" class="form-inline">
    <input name="filter" class="form-control" placeholder="filtro" value="@Model.RouteValue["Filter"]"/>
    <button type="submit" class="btn btn-info">
        <span class="glyphicon glyphicon-search" aria-hidden="true"></span>Procurar
    </button>
</form>

Total de Pedidos: @Model.TotalRecordCount
<div>
    <vc:pager paging-list="@Model"></vc:pager>
</div>
@*<p>
    <a asp-action="Create">Create New</a>
</p>*@
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Cliente.Nome)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Cliente.Nome)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CarrinhoCompra.CarrinhoCompraID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CarrinhoCompra.CarrinhoCompraID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PedidoTotal)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PedidoTotal)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TotalItensPedido)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TotalItensPedido)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                    @Html.DisplayFor(modelItem => item.Cliente.Nome)
            </td>
            <td>
                    @Html.DisplayFor(modelItem => item.CarrinhoCompra.CarrinhoCompraID)
            </td>
            <td>
                    @Html.DisplayFor(modelItem => item.PedidoTotal)
            </td>
            <td>
                    @Html.DisplayFor(modelItem => item.TotalItensPedido)
            </td>
            <td>
                @*<a asp-action="Edit" asp-route-id="@item.PedidoId">Edit</a> |*@
                <td>
                    @*<form asp-action="Edit" asp-route-id="@item.Id" class="form-check-inline">
                            <input type="image" src="/imagens/editar.png" alt="submit" style="width:auto" class="nav-link btn btn-link py-0">
                    </form>*@

                    <form asp-action="Details" asp-route-id="@item.PedidoId" class="form-check-inline">
                        <input type="image" src="/imagens/detalhes.png" alt="submit" style="width:auto" class="nav-link btn btn-link py-0">
                    </form>

                    <form asp-action="Delete" asp-route-id="@item.PedidoId" class="form-check-inline">
                        <input type="image" src="/imagens/excluir.png" alt="submit" style="width:auto" class="nav-link btn btn-link py-0">
                    </form>
                 </td>
                
                <a asp-action="=PedidoFilmes" asp-route-id="@item.PedidoId">Ver Itens</a>
            </td>
        </tr>
}
    </tbody>
</table>
